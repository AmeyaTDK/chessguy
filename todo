1. change logic from switch case to subtraction. 1
2. fix indentation 1
3. fix variable names 1
4. tabstop=4 1
5. change char[] to enum for piece type. 1

== source piece detection ==

1. parse input and print format.
2. get_destination_square(char s[]) => square
3. write  source finder() for all 6 pieces (find source square)
    -create cache(linked list) for all  piece status
	-initialize cache with all initial pieces
	-implement source finding algo using qualifiers(empty,row/col,row and col)
   
==

input format:
[piece][destination] eg.Ra4
[destination] eg. d4

Ra4 => [R][a4]
d4 => [P][d4]

==
implement linked list


